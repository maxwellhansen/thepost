- Error/Bounds checking for input
	* throw an exception?

- Allow the user to change newsgroup(/address?) in the loop (going "back")

- Allow the user to authenticate to a password-protected server
	* determine if server requires authentication

- Search through headers for a string (regexp or exact matches?)

- Ability to post to articles

- Add a sweet GUI
